"""Experiment-only integrations kept separate from the public estimators.

The modules in this package are deliberately optional: they may depend on a
benchmark runner such as TabArena, while importing :mod:`tabicl` itself must
remain lightweight.
"""

